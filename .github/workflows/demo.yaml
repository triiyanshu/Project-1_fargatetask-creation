name: 'Books Dev Terraform3'
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Test/Prod'
        required: true
        #default: 'Default value'
  push:
    branches: 
      - main
      - ISPB-4.0.1 
